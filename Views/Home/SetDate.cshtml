<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>研討會日期設定</title>
    <link rel="icon" href="./images/yes.png" type="img/123.jpg" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="./CSS/main.css">

    <style>
        body {
            font-family: Arial, sans-serif;
        }

        h2 {
            padding: 30px;
            text-align: center;
            font-size: 40px;
            margin-bottom: 3%;
        }

        #addedDates {
            text-align: center;
            font-size: 20px;
        }
    </style>
</head>

<body>

    <h2>研討會日期設定</h2>

    <div class="container">
        <table class="table table-bordered">
            <thead>
                <tr>
                    <th scope="col">日期</th>
                    <th scope="col">時間</th>
                    <th scope="col">地點</th>
                    <th scope="col">操作</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td><input type="date" class="form-control" id="newDate" name="date"></td>
                    <td><input type="time" class="form-control" id="newTime" name="time"></td>
                    <td><input type="text" class="form-control" id="newLocation" name="location"></td>
                    <td><button type="button" class="btn btn-primary" onclick="saveDate()">儲存</button></td>
                </tr>
            </tbody>
        </table>
    </div>

    <div id="addedDates">
        <!-- 這裡會動態顯示新增的日期 -->
    </div>

    <script>
        function saveDate() {
            // 獲取新的日期、時間和地點
            var newDate = document.getElementById('newDate').value;
            var newTime = document.getElementById('newTime').value;
            var newLocation = document.getElementById('newLocation').value;

            // 創建新的表格行
            var newRow = document.createElement('tr');
            newRow.innerHTML = '<td>' + newDate + '</td>' +
                '<td>' + newTime + '</td>' +
                '<td>' + newLocation + '</td>' +
                '<td></td>'; // 空白列

            // 將新行添加到表格中
            document.querySelector('tbody').appendChild(newRow);

            // 清空輸入欄位
            document.getElementById('newDate').value = '';
            document.getElementById('newTime').value = '';
            document.getElementById('newLocation').value = '';

            // 顯示新增的日期
            document.getElementById('addedDates').innerHTML += '新增的日期：' + newDate + '<br>';
        }
    </script>
</body>

</html>
